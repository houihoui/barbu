version: '3.8'

services:
  # PostgreSQL local pour développement
  postgres:
    image: postgres:16-alpine
    container_name: barbu-postgres
    environment:
      POSTGRES_DB: barbu_dev
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  # API Backend (à configurer plus tard)
  # api:
  #   build:
  #     context: ../backend
  #     dockerfile: src/Barbu.Api/Dockerfile
  #   container_name: barbu-api
  #   ports:
  #     - "5001:8080"
  #   environment:
  #     ASPNETCORE_ENVIRONMENT: Development
  #     ConnectionStrings__DefaultConnection: "Host=postgres;Database=barbu_dev;Username=postgres;Password=postgres"
  #   depends_on:
  #     postgres:
  #       condition: service_healthy

  # Frontend (à configurer plus tard)
  # frontend:
  #   build:
  #     context: ../frontend/barbu-nuxt
  #     dockerfile: Dockerfile
  #   container_name: barbu-frontend
  #   ports:
  #     - "3000:3000"
  #   environment:
  #     NUXT_PUBLIC_API_BASE: http://api:8080
  #   depends_on:
  #     - api

volumes:
  postgres-data:
